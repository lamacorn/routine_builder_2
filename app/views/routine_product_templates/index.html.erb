<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All routine products
    </h1>

    <a href="/routine_products/new" class="btn btn-block btn-outline-secondary">
      Add a new routine product
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Routine
        </th>

        <th>
          Product
        </th>

        <th>
          Use frequency
        </th>

        <th>
          Note
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @routine_products.each do |routine_product| %>
      <tr>
        <!-- Display routine_product.id start -->
        <td>
          <%= routine_product.id %>
        </td>
        <!-- Display routine_product.id end -->

        <!-- Display routine_product.routine_id start -->
        <td>
          <% if routine_product.routine.present? %>
            <a href="/routines/<%= routine_product.routine.id %>">
              <%= routine_product.routine.customer_id %>
            </a>
          <% end %>
        </td>
        <!-- Display routine_product.routine_id end -->

        <!-- Display routine_product.product_id start -->
        <td>
          <% if routine_product.product.present? %>
            <a href="/products/<%= routine_product.product.id %>">
              <%= routine_product.product.name %>
            </a>
          <% end %>
        </td>
        <!-- Display routine_product.product_id end -->

        <!-- Display routine_product.use_frequency_id start -->
        <td>
          <% if routine_product.use_frequency.present? %>
            <a href="/usefrequencies/<%= routine_product.use_frequency.id %>">
              <%= routine_product.use_frequency.description %>
            </a>
          <% end %>
        </td>
        <!-- Display routine_product.use_frequency_id end -->

        <!-- Display routine_product.note start -->
        <td>
          <%= routine_product.note %>
        </td>
        <!-- Display routine_product.note end -->

        <!-- Display routine_product.created_at start -->
        <td>
          <%= time_ago_in_words(routine_product.created_at) %> ago
        </td>
        <!-- Display routine_product.created_at end -->

        <!-- Display routine_product.updated_at start -->
        <td>
          <%= time_ago_in_words(routine_product.updated_at) %> ago
        </td>
        <!-- Display routine_product.updated_at end -->

        <td>
          <a href="/routine_products/<%= routine_product.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @routine_products, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
