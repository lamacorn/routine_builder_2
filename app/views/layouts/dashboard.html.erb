<div class="jumbotron">
  <h1 class="display-3">GRWMD Routine Builder</h1>
  <p class="lead">GRWMD makes straightforward skincare for men. 

</p>
  <hr class="my-4">
  <p>The GRWMD Routine Builder is a prototype CRM for GRWMD skincare coaches to easily develop custom routines for customers during consultations, track customer retention and success, and track data on the efficacy of products over time.</p>
  <!--<p class="lead">-->
  <!--  <a class="btn btn-primary btn-sm" href="http://www.grwmd.com" role="button">Learn more</a>-->
  <!--</p>-->
</div>


<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <h3>Recent Customers</h3>
                <ul class="list-group">
                  <% Customer.all.order({ :updated_at => :desc }).first(3).each do |a_user| %>
                  <li class="list-group-item d-flex justify-content-between align-items-center">
                    <a href="/customers/<%= a_user.id %>"><%= a_user.firstname %> <%= a_user.lastname %></a>
                 
                  </li>
                  <% end %>
                </ul>
        </div>
        <div class="col-sm-4">
            <h3>Top Concerns</h3>
                <ul class="list-group">
                  <% Concern.all.order({ :name => :asc }).each do |a_concern| %>
                  <% if a_concern.customer_count > 0  %>
                  <li class="list-group-item d-flex justify-content-between align-items-center">
                    <a href="/concern/<%= a_concern.id %>"><%=   a_concern.name %></a>
                    <span class="badge badge-primary badge-pill"><%= a_concern.customer_count %></span>
                  </li>
                  <% end %>
                </ul>
                <% end %>
        </div>
        <div class="col-sm-4">
            <h3>Top Products</h3>
                <ul class="list-group">
                  <% Product.all.order({ :name => :asc }).each do |a_product| %>
                  <% if a_product.routine_count > 0  %>
                  <li class="list-group-item d-flex justify-content-between align-items-center">
                    <a href="/product/<%= a_product.id %>"><%=   a_product.name %></a>
                    <span class="badge badge-primary badge-pill"><%= a_product.routine_count %></span>
                  </li>
                  <% end %>
                </ul>
                <% end %>
            

        </div>
    </div>
</div>


